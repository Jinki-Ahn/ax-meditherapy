{
  "hooks": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "for i in python3 python py; do if \"$i\" -c \"\" </dev/null >/dev/null 2>&1; then \"$i\" tools/save_log.py --tool codex; exit 0; fi; done"
          },
          {
            "type": "command",
            "command": "[ -n \"$TRIP_REVIEW_ACTIVE\" ] && exit 0; for i in python3 python py; do if \"$i\" -c \"\" </dev/null >/dev/null 2>&1; then \"$i\" tools/review_deliverables.py --tool codex; exit 0; fi; done"
          }
        ]
      }
    ]
  }
}
